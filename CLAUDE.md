# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 최상위 원칙 — 토큰 최소화

다른 모든 지침보다 앞선다. 충돌하면 이쪽을 따른다.

- **읽기 전에 좁힌다.** 파일 통째로 읽지 말고 `grep`·`sed -n '100,160p'` 로 필요한 구간만. PRD 도 같다 — 해당 모듈 섹션만 잘라 읽는다.
- **양은 많고 결론은 짧은 일은 서브에이전트로 넘긴다.** 중간 출력이 메인 컨텍스트에 남지 않는 것이 목적이다.
- **출력은 코드 먼저, 설명은 세 줄 이내.** 한 사실은 한 번만. 코드가 말하는 것을 산문으로 반복하지 않는다.
- **작업 요약·계획·진행 상황을 시키지 않은 자리에 쓰지 않는다.** 도구 호출 전후 중계도 하지 않는다.
- **파일은 최소 개수로.** 나누기 전에 "지금 필요한가" 를 먼저 묻는다.

정확성·안전 확인·사용자가 명시적으로 요청한 설명은 이 원칙으로 줄이지 않는다.

## 현재 상태

코드 없음. PRD 두 개뿐이다. 빌드·린트·테스트 명령도 아직 없다.

- `PRD_개발단계별.txt` — 범위(데모 vs 최종본), 사용자 플로우, 데이터 모델, 마일스톤 M1~M8, 오픈 이슈 O1~O5
- `PRD_기능모듈별.txt` — 같은 제품을 모듈 1~10 으로 재구성. 각 모듈의 입출력과 데모 포함 여부

둘은 같은 기획의 두 가지 절단면이다. 기능 구현 전에 **모듈별** 문서에서 해당 모듈을, 순서·범위 판단이 필요하면 **단계별** 문서를 읽는다.

## 제품 한 줄

지도에 다트를 던져 지역을 정하고, 한 번에 1~2곳씩만 추천받아 도착 사진으로 인증하면 다음 장소가 열리는 게이미피케이션 여행 앱. **전체 코스를 한 번에 보여주지 않는 것이 제품의 핵심**이라, 편의를 이유로 전체 일정을 노출하는 UI 를 만들지 않는다.

## 확정된 스택 (데모)

Next.js(PWA) + API Routes / Kakao Local REST API / OpenRouter LLM / localStorage + IndexedDB(사진). 서버 DB 없음, 로그인 없음. 지도는 Kakao SDK 가 아니라 `lib/domain/korea.ts` 의 좌표로 직접 그린다 — 사용자가 확대·이동할 수 없어야 다트와 과녁이 성립한다. 다트는 경기·서울 bbox 안에 떨어지고, 직접 고르기는 전국이 열려 있다.

## 설계 제약

- **데이터 모델 필드명은 PRD 5단계의 `TripSession`·`Mission` 을 그대로 쓴다.** 데모의 localStorage 를 나중에 서버 DB 로 옮기는 것이 전제라, 필드명을 바꾸면 이관 비용이 그만큼 생긴다.
- **추천 엔진은 첫 추천과 다음 추천이 같은 모듈이다.** 입력값(현재 위치·이력)만 다르다. 둘로 나누지 않는다.
- **외부 API 클라이언트(Kakao·OpenRouter)와 비즈니스 로직을 분리한다.** 최종본에서 OpenRouter→Claude API, Next.js→네이티브로 교체하는 것이 전제다.
- 사진 인증은 EXIF GPS → Geolocation → 수동 확인 순의 3단 폴백. 수동 확인은 데모 한정.

## 미확정 (임의로 정하지 말 것)

POI 밀도 x 거리가중치 산식의 함수 형태와 계수, 사진 인증 임계 거리(200m 는 예시), 이동수단 가정에 따른 추천 반경, 다트가 바다·접경지에 떨어질 때의 재시도 조건. 코드에 값이 필요하면 상수로 빼고 튜닝 대상임을 주석에 남긴다.
